{
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start:db": "json-server --watch db.json",
    "start:parcel": "parcel index.html",
    "start": "concurrently npm:start:*"
  }
}
